stages:
  - build-and-push-images
  - test-build
  - provision-database
  - update-app-service

build_image:
  stage: build-and-push-images
  image: docker:20.10
  services:
    - docker:20.10-dind
  variables:
    DOCKER_PASS: ""
  before_script:
    - apk add py3-pip gcc musl-dev python3-dev libffi-dev openssl-dev cargo make
    - pip install --upgrade pip
    - pip install azure-cli
    # - DOCKER_PASSWORD=$(az keyvault secret show --name "acr-admin-password" --vault-name "ecc-dev-kv-de123d9" --query "value")
    - docker login -u${DOCKER_USER} -p${DOCKER_PASSWORD} ${DOCKER_REGISTRY}
  script:
    - docker build -t ${DOCKER_REGISTRY}/drupal-fpm:latest -t drupal-fpm -f Dockerfile-drupal .
    - docker build -t ${DOCKER_REGISTRY}/nginx-drupal:latest -t nginx-drupal -f Dockerfile-nginx .
    - docker push ${DOCKER_REGISTRY}/nginx-drupal
    - docker push ${DOCKER_REGISTRY}/drupal-fpm
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
    
# test-build:
#   stage: test-build
#   image: docker:20.10
#   services:
#     - docker:20.10-dind
#   variables:
#     DB_CONN_TEST: ""
#   script:
#       | 
#     # Performs a basic test on the drupal image to ensure it can connect to the Database
#         - echo 'Test drupal image for database connectivity....'
#         - docker run -t -e MYSQL_DATABASE=${MYSQL_DATABASE} -e MYSQL_HOST=${MYSQL_HOST} -e MYSQL_USER=${MYSQL_USER} -e MYSQL_PASSWORD=${MYSQL_PASSWORD} ${DOCKER_REGISTRY}/drupal-fpm:latest vendor/drush/drush/drush st >> DBTestOutput.txt
#         - DB_CONN_TEST=$(cat DBTestOutput.txt | grep -E '^Database\s+:\sConnected')
#         - if [ -z "$DB_CONN_TEST" ]; then echo "Database connection error in drupal container\n" && exit 1; fi
#     artifacts:
#     paths:
#       - DBTestOutput.txt

provision-database:
  stage: provision-database
  script:
    - echo "To do - Provision database scripting when confirmed with devs"
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'

update-app-service:
  stage: update-app-service
  script:
    - echo "Container app is restarting - environment will be down temporarily"
    - az container restart --name lgDrupalContainerGroup --resource-group ecc-drupal-gov-dev
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      when: manual