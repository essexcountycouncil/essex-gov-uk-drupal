variables:
  css:
    base:
      # This is called in your essex.info.yml file via `libraries-extend`.
      # This ensures that in the cascade, your variables come after the base theme's
      # variables.
      css/variables.css: {}

# To override CSS from the base theme, add your library here, and then use
# `libraries-extend` to add that code to the library from the base theme. See the
# essex.info.yml file for an example, using this `sample-library`.

overrides:
  css:
    theme:
      css/overrides.css: {}

fonts:
  css:
    theme:
      css/fonts.css: {}

page-sections:
  css:
    theme:
      css/components/page-sections.css: {}

featured-teasers:
  css:
    theme:
      css/components/featured-teasers.css: {}

events-listing-page:
  css:
    theme:
      css/components/events-listing-page.css: {}

add-to-calendar:
  css:
    theme:
      css/components/add-to-calendar.css: {}
  js:
    js/add-to-calendar.js: {}
  dependencies:
    - core/drupal
    - core/once

# @TODO: remove this library when
# https://github.com/localgovdrupal/localgov_base/issues/518
# is resolved
edit-tabs:
  css:
    theme:
      css/components/edit-tabs.css: {}

# sample-library
#   css:
#     theme:
#       css/overrides.css: {}

# If your CSS is for a new component, and is not going to override existing CSS, you
# can add that to your component in the usual ways - via `{{ attach_library }}` or a
# preprocess hook, or similar approach.

# sample-library-2
#   css:
#     theme:
#       css/sample.css: {}
